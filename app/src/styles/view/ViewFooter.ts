import { hstack } from "panda/patterns";

const root = hstack({
  w: "full",
  h: "fit-content",
  justify: "space-between",
  alignItems: "center",
  p: 4,
  gap: 4,
});

export { root };
export default root;
